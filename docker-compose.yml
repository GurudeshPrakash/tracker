version: '3.8'

services:
  tracker:
    build: .
    container_name: flux-tracker
    restart: unless-stopped
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./backups:/app/backups
    environment:
      - PORT=8501
