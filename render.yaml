services:
  - type: web
    name: flux-tracker
    runtime: docker
    plan: standard
    region: oregon
    healthCheckPath: /api/health
    disk:
      name: tracker-data
      mountPath: /app/data
      sizeGB: 1
    envVars:
      - key: PORT
        value: 8501
